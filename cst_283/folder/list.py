# Example tuple
my_tuple = (1, 2, 3, 4, 1, 2, 1, 3, 4, 5)

# Element to check
element = 1

# Count the number of occurrences
count = my_tuple.count(element)

# Print the result
print(f"The element {element} occurs {count} times in the tuple.")
